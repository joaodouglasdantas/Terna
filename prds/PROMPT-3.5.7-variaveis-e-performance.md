Você está no vault (C:\laragon\www\vault), mestre do harness em `projetos/referencias/harness` (3.5.6, publicada 16/09/2026 de madrugada; relatório em `prds/RELATORIO-3.5.6-2026-09-16.md`, achados em `prds/MONITOR-EXECS-2026-09-16-madrugada.md` E1–E7 e `prds/MONITOR-EXECS-2026-09-15-noite.md` D1–D18, estado e pendências em `prds/HANDOFF-2026-09-16-madrugada.md` §1). Memória relevante: `project_harness_356_madrugada_16set`, `project_harness_dt010_pool_duelo`, `feedback_harness_env_versionado_sem_drift`, `feedback_harness_versao_convencao_patch`, `reference_bash_tool_heredoc_limites`.

OBJETIVO DESTA SESSÃO — harness 3.5.7 com duas frentes, nesta ordem:

FRENTE A — NENHUMA DECISÃO DO HARNESS VIVE EM VARIÁVEL LOCAL. Tudo que é decisão do harness viaja com o mestre pelo sync; local só o que é do projeto ou da máquina. Motivo medido em 16/09: o `harness.env` do mestre tem 75 chaves ativas e o da Mariana só 19 — 58 chaves de decisão (HARNESS_DUELO_*, HARNESS_GUARD_*, HARNESS_PIPELINE_MAX_VIVOS*, HARNESS_SEQ_FAIXAS*, HARNESS_MIG_NUMERACAO, HARNESS_EXTERNAL_REVIEWER, HARNESS_DELEGATE_*, HARNESS_NOTURNO*, HARNESS_WATCHDOG, HARNESS_FRENTES, HARNESS_DAEMON, HARNESS_PRESENCE_*…) simplesmente não existem nos projetos e caem no default do CÓDIGO, que nem sempre é o que o mestre declara; 10 chaves divergem (RAG_ENABLED, VERBOSIDADE, LINT_CMD, templates de worktree — estas são legitimamente do projeto); o `~/.harness.env.local` do PC sobrescreve HARNESS_DUELO_MODELS/SUPLENTES, HARNESS_OLLAMA_MODEL e HARNESS_FRENTES_MAX (só deveria ter OPENROUTER_API_KEY, HARNESS_BEHOLDER_URL/TOKEN); dos 30 projetos no PC só a Mariana está na 3.5.6, 20 na 3.4.34, 9 entre 2.8.0 e 3.4.28. A 3.5.6 deu o primeiro passo (DT-010: `hooks/_defaults.env` versionado com a pool do duelo, sourceado antes do harness.env por bash e pelos .mjs) — generalize:
1. Inventário e classificação de TODAS as chaves (harness.env do mestre, defaults em código dos hooks/skills, harness.env.local.example, settings.json): três camadas — (a) DECISÃO DO HARNESS → `hooks/_defaults.env` (versionado, sync, único lugar); (b) DO PROJETO → `.claude/harness.env` (DB/URL/porta, lint, templates de worktree, RAG on/off, verbosidade, targets) com LISTA FECHADA de chaves permitidas; (c) DA MÁQUINA → `harness.env.local` / `~/.harness.env.local` (segredos, URLs de presença, ollama, modelos locais) com LISTA NEGRA: chave de decisão definida ali é override consciente e o doctor avisa.
2. Mecânica: todo script/mjs carrega `_defaults.env` → `harness.env` → `.local` → `~/.local` na mesma ordem (hoje cada hook tem seu loop; centralize num `_env.sh`/`presence.mjs makeCtx` e cubra harness-metrics.sh, guard-migration.sh, guard-write.sh, task-packet.sh, harness-worktree.sh, harness-doctor.sh, harness-ui.mjs); o default em código deixa de existir onde `_defaults.env` declara (ou vira igual, com teste que compara).
3. `harness-sync.sh`: linhas `ENV|decisao-no-projeto|<chave>|<arquivo>` (decisão do harness definida em camada errada) e `ENV|obsoleta|<chave>` (chave que o mestre não conhece mais); `--apply` comenta a chave de decisão no harness.env do projeto quando igual ao default (migração automática) e preserva quando diferente (override consciente, reportado); painel do `/deus` mostra o drift por projeto.
4. `harness-doctor.sh`: seção "Camadas de configuração" — origem resolvida de cada chave de decisão (defaults|projeto|local|máquina), WARN por override, FAIL por chave de projeto obrigatória ausente; `harness-config.html`/`harness-ui.mjs` gravam na camada certa.
5. O `harness.env` do MESTRE vira só template das chaves de projeto (ou `harness.env.example`); as decisões saem dele para `_defaults.env`. ONBOARDING 3.5.7 com a regra e a migração; CHANGELOG; suíte `tests/t-357-camadas.mjs` (sandbox como t-355/t-356) provando: chave de decisão só no defaults chega ao projeto; override local acusa; obsoleta acusa; ordem de carga igual em bash e mjs. t-354/355/356 continuam verdes.
6. Só DEPOIS disso rode o `/deus` nos 29 projetos (o Charles decide a ordem; caronte/sagittarius/palantir primeiro), e só depois do /deus apague HARNESS_DUELO_*/HARNESS_OLLAMA_MODEL/HARNESS_FRENTES_MAX do `~/.harness.env.local` do PC.

FRENTE B — 5 MELHORIAS DE PERFORMANCE GERAL, escolhidas entre estas candidatas MEDIDAS (cada uma com prova, melhoria esperada medível e como validar, como no relatório da 3.5.6):
- E6: semáforo de frentes liberou a vaga da 144 com a exec viva em Fase 2 (heartbeat só renova no despacho de hefesto/dedalo; 45 min de review = slot expira) → renovar em todo Agent com run viva e expirar só por sessão morta (`sessoes.mjs`).
- E5: exec noturna fechou 7 tasks sem commit por task (1 commit no fim, 27 arquivos no working tree) → commit por task como passo mecânico do fechamento na `/prd-exec` + `guard-agent --post` avisando task ✅ sem commit.
- E7/140-b: `node_modules` ausente na main e nas worktrees ("playwright instalado no pré-flight") → `harness-worktree.sh novo` faz `npm i --no-save` (5 s) ou junction para o node_modules do principal; doctor acusa @playwright/test ausente.
- 144 review: Codex em limite → SOLO-2 em 3 partes = 6 sherlocks + michelangelo, ~30 min de Fase 2 → com Codex fora, 1 sherlock por parte + 1 de costura (medir ciclos e bloqueantes achados por sherlock nas runs de 15–16/09).
- B1: hooks de prompt a 2–3 s com 3 abas abrindo juntas (metrics-auto) e cancelamentos sob carga → estender o daemon (hoje Bash/presença) a guard-agent/guard-write/task-telemetry; medir spawn com `carga-maquina.sh`.
- E3: correção de documento pós-gate custou 4 turnos de Edit errado → hermes por documento após o gate (Modo C) como padrão na `/prd` Passo 10.
- Paralelismo travado por dependências em TODAS as execs (`limitou=dependencias`, fator 1,18–1,29, vivos_max 4–5) → `task-packet.sh --check` / `/prd` Passo 8 medem profundidade do grafo × largura e sugerem tasks disjuntas.
- Acceptance como task (140-b TASK-008: 21 + 11 min, 2 PARCIAL) → validar se o teto por spec da 3.5.6 resolve; se não, a pai roda a família em vez de despachar.
Use `tests/analise/monitor/compara-prds.js "C:\laragon\www\dra-mariana-duarte" 10` e `/harness-report` como régua antes/depois; a telemetria está em `dra-mariana-duarte/prds/_metrics/`.

REGRAS: nunca push (Mariana = deploy; vault também sem push sem o Charles); versão é PATCH (3.5.7) e não bumpe sem confirmar — o Charles roda sessões paralelas; commits escopados; nunca commitar edições alheias; o Bash desta ferramenta engole `\\` em heredoc e quebra acima de ~10 KB — use Write/Edit para arquivos grandes; toda mudança com caso na suíte; `bash -n`/`node --check` em tudo; sincronize na Mariana só com nenhuma exec viva (`sessoes.mjs`); relatório final em `prds/RELATORIO-3.5.7-<data>.md` e nota de monitor se houver exec para vigiar.

PENDÊNCIAS HERDADAS (do Charles, não desta sessão, mas não deixe se perder): push da Mariana pelo `dra-mariana-duarte/prds/GUIA-PRE-DEPLOY-2026-09-16.md` (39 commits; 0200 mexe em dado — backup antes; DT-598 já corrigido em a5b9ba5e); DT-600 + `HARNESS_PW_LOCK_ESPERA=on` na Mariana; pastas `--wt-*` presas pelas abas.
