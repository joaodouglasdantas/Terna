{
  "name": "@terna/cliente",
  "version": "0.1.0",
  "private": true,
  "description": "O jogo em si: roda no navegador (e, mais tarde, dentro do app de PC).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource/tiny5": "^5.3.0",
    "@terna/compartilhado": "*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
