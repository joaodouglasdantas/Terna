# Usado só no `npm run build` (o site publicado). Endereço do servidor no Render: se o
# Render der outro nome ao serviço (ex.: terna-servidor-x1y2.onrender.com), troque aqui.
VITE_API_URL=https://terna-servidor.onrender.com
